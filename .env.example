# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# One database, three schemas (platform, ref, ryc). A role per schema is granted
# in migrations: see docs/design/architecture-style.md section 8.
DATABASE_URL="postgresql://studens:CHANGE_ME@localhost:5432/studens?schema=platform"
